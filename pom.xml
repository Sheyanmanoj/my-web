pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                script {
                    sh 'mvn clean install -f path/to/your/pom.xml'
                }
            }
        }
    }
}
